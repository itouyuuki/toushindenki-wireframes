# 東進電機工業様 サイトリニューアル — ワイヤーフレーム

**このファイルは「現在地」を書く場所。** 判断基準・流儀・見送った案の理由は memory 側に書く。
作業の区切りごとに、下の「現在地」と「未決事項」を更新すること。

## この案件は何か

企画書で開発を2工程に分けており、ここは**設計・デザイン工程**（25万＋税）の成果物。
開発工程（40〜60万＋税）では**原則として仕様の追加・変更・削除を行わない**契約なので、
確認事項は設計段階で潰しきる必要がある。実装はWordPress（カスタムタクソノミー前提）。

- 窓口＝真田様（セキュリティ事業部・実質の決裁者）。宮下社長はウェブの打合せに出ない
- ワイヤーは**印刷して番号を指しながら**打合せする。ブロックの並べ替えは番号がズレるので注意

## 現在地（2026-08-18）

- 2026-08-05 に真田様と全ページレビュー実施。決定事項は `docs/2026-08-05_打合せ決定事項.md`
- 反映済み：注釈の全削除／製品情報ページの型番検索UI／ダウンロードの問い合わせ欄削除／
  「代理店」→「協力店」の全面統一／全7ページのPDF出力（`pdf/`）
- 宮下社長宛のメール文は作成済み・**未送信**
- **2026-08-17 社内会議（技術顧問と）で全ページの実装方針を検討。** 音声＝
  `~/Downloads/NEXT RESERVATION 本社 21.m4a`、書き起こし＝`~/Downloads/社内会議2026年8月17日.txt`

### 2026-08-17 に決まった実装方針

- **データ設計は型番のタクソノミーで串刺し。** 製品投稿／カタログ・資料／アクセサリーに同じタームを付け、
  製品詳細の「資料ダウンロード」「対応アクセサリー」を自動生成する
- **新製品は「新製品」タグで拾う。** トピックス専用の投稿は作らない
- カテゴリー＝階層あり（分類軸）、タグ＝階層なし（横断で拾う飛び道具）で使い分ける
- **本文（the_content）は1ページに1箇所しか置けない。** 製品詳細は
  ファーストビュー＝コーディング／仕様まわり＝コンテンツ／資料・アクセサリー＝コーディング
- **特徴アイコンの正方形は却下。** 文字数で折り返しが破綻するため幅可変のテキストラベルにする
- **問い合わせフォームの項目は減らす方向で提案。** 項目を増やしても迷惑メールは減らない
- 会社案内の 01/02 レイアウトは固定ページ＋CSSクラス対応付け。会社概要の表は WP のテーブル

判断の根拠・見送った案の理由は memory 側（`toushin-*`）に記載。

### 次にやること

- **ワイヤーにコーディング領域／コンテンツ領域の色分けを入れる**（未着手）
- 型番検索・CF7自動入力・メディアのカスタムフィールドを**技術検証**したうえで社内会議を再度実施
- メールで詰められる確認事項は先に潰す

## 未決事項

1. **型番検索の実装方法と見積もり** ← 先方の最重要要望。別途見積もりの可能性を伝達済み。
   3案（型番タクソノミー／Googleサイト内検索／カスタムフィールド）とも一長一短。**要技術検証**
2. **製品ページからの問い合わせ自動入力**（型番・品名がフォームに入る）の実装可否。
   方針は CF7 のサーバー側フックで GET を受ける形に決まったが**動作未検証**
3. **電子機器組立事業ページを作るか** ← 宮下社長にメールで確認待ち。不要なら工数を1.に回す
4. **お問い合わせ件名に「製造委託／基板実装」を足すか** ← 同じメールで確認
5. **製品カテゴリーの反映漏れ** — ヘッダーは決定した7項目だが、**フッターの製品情報列と
   `assets/wire.js` の `CATS` 配列が現行サイトの8項目のまま**。この2箇所を揃える
6. 製品一覧・ダウンロード一覧の見せ方（初期表示なし／カテゴリー選択後に出す方針）
7. **カタログ差し替え時の URL 問題**をどう回避するか（メディア機能／独自実装）
8. **プラグイン依存する機能の、アップデートで壊れたときの修正責任**を契約に明記する

## ファイル構成

```
*.html          ワイヤー7ページ（index / products / product-detail /
                downloads / assembly / contact / company）
assets/wire.css 画面用＋印刷用CSS。@media print はA4横・右に書き込み欄
assets/wire.js  スマホのドロワーメニューを生成。カテゴリーや問い合わせ項目の
                配列がここにあるので、文言変更のとき見落としやすい
docs/           打合せの決定事項
pdf/            全7ページのPDF（Chromeヘッドレスで生成）
ログ/            打合せ・社内会議の音声書き起こし
```

**リポジトリ**: `https://github.com/itouyuuki/toushindenki-wireframes`
実体は `C:\Users\itou\toushindenki-wireframes`。2026-08-18 に
`C:\xampp\htdocs\toushindenki-wireframe` から移動した（htdocs 配下ではないので XAMPP では配信されない。
確認は `file:///` で開く）。

## PDFの作り直し

`pdf/` の実ファイル名は `01_トップ.pdf` 形式なので、下のループで出したあとリネームする。

```bash
for f in index products product-detail downloads assembly contact company; do
  "/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu \
    --no-pdf-header-footer --virtual-time-budget=5000 \
    --print-to-pdf="C:/Users/itou/toushindenki-wireframes/pdf/$f.pdf" \
    "file:///C:/Users/itou/toushindenki-wireframes/$f.html" 2>/dev/null
done
```
